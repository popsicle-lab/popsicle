---
agent_context: [Project preferences]
- 界面 / Agent 语言：简体中文
- 产品目录：`products/`
- ADR：`products/<product>/decisions/adr/`
- PDR：`products/<product>/decisions/pdr/`
- Pipeline 审批：delegate-dangerous（危险操作需审批（其余代批））
- 创建或更新 Issue / 文档时，`--title` 与 `--description` 使用简体中文（除非用户明确要求英文）。
- 非危险 `requires_approval` 阶段可由 agent 代批完成；危险阶段（`cutover`、`living-docs`）仍需用户显式 `--confirm`。

[Project context]
# Project Context

> **Last-Updated**: 2026-06-26
> **Last-Decision-Ref**: ADR-001
> **Owner**: 仓库维护者 + Settings UI；§现在状态 由 doc-sync-weekly 刷新

`docs/PROJECT_CONTEXT.md` 是本仓库**唯一的工程画像权威源**（git 追踪）。Agent 在 `inject_on_run: true` 时通过 `issue start` / `doc create` 的 `agent_context` 注入 §工程画像（不含 §现在状态）。

## 工程画像

### Tech Stack

- **语言**：Rust（workspace，`rust-toolchain.toml` pin）
- **前端**：Tauri 2 + React + TypeScript（`ui/`，`cargo build --features ui -p cli-ux`）
- **形式化**：IntentLang + Z3（`intent-validate` / `make intent`）
- **存储**：SQLite @ `.popsicle/state.db`（ADR-013 / ADR-032）

### 仓库布局（目标态）

| 路径 | 职责 |
|---|---|
| `crates/skill-runtime/` | Skill 加载、Pipeline 会话、Issue 实体 |
| `crates/artifact-system/` | Document、Guard、Context 装配 |
| `crates/cli-ux/` | `popsicle` CLI + Tauri IPC + telemetry inject |
| `crates/telemetry/` | Agent 观测旁路：WAL + OTLP export（fail-open） |
| `crates/storage/` | DocumentRow / WorkspaceStore |
| `products/<product>/` | IDD 四件套（PRODUCT / ARCHITECTURE / intents / decisions） |
| `migration/` | 切流 traceability + progress 看板（历史归档） |
| `docs/` | CHARTER、PROJECT_CONTEXT、baseline（含 legacy 冻结快照） |

Workspace members：`crates/*`（ADR-003）。

### DevOps 标准

- **本地门禁**：`make check`（fmt + clippy + test，`-Dwarnings`）
- **Golden**：`make golden`（self-host 回归链，见 `docs/baseline/`）
- **Intent**：`make intent` / `popsicle tool run intent-validate path=products`
- **Hooks**：`make install-hooks`（pre-commit fmt/clippy/test）
- **CI/Release**：GitHub Actions 纯 Rust 矩阵 + 可选 UI job（ADR-014）
- **安装**：`scripts/install.sh`（无 UI/completions defer）

### 关键约束

- Pipeline 审批：`workflow.approval_mode`（默认 `delegate-dangerous`）；危险阶段 `cutover`、`living-docs`
- Spec 门禁：新能力须 `feature-spec` / `migration-slice-spec` → `feature-delivery` / `migration-slice-delivery`；`fix-regression` 不得滥用（PROJ-53）
- 活文档：只用现在时；历史进 ADR/PDR / `migration/traceability.md`
artifact: implementation-coverage
slug: phase-c-agent-telemetry
generated_by: shadow-implementer
slice: telemetry
last_updated: 2026-06-26
crate: crates/cli-ux/ + products/telemetry/
cargo_test_exit: 0
intent_blocks_total: 1
intent_blocks_covered: 1
query_anchors:
  - "Agent 怎么上报 score？"
  - "doc check 通过后 telemetry_hint 在哪？"
doc_type: shadow-implementer
id: doc-161
pipeline_run_id: 00000047-0000-4047-8004-47000000000047
status: active
title: Phase C Agent 上报约定与 doc check hint
version: 1
---

# 实现覆盖报告 — Phase C Agent 上报约定

> PROJ-72 / T-TEL-0002：Agent 显式 `tool run telemetry` 约定 + `doc check` 通过时 JSON `telemetry_hint`。

## Scope Checklist

- [x] target slice 已确认（telemetry 产品 + cli-ux 编排层）
- [x] Phase C intent block 已枚举（T-TEL-0002 → TelemetryRecordFailOpen）
- [x] File Manifest 路径清单已列出
- [x] 每个 block 已标 已实现/部分/缺失
- [x] 跨界端口归属已核对（WAL 在 crates/telemetry，hint 在 cli-ux）

## Summary

| 指标 | 值 |
|---|---|
| Slice | telemetry |
| Crate / 路径 | `crates/cli-ux/`、`crates/telemetry/`、`products/telemetry/` |
| acceptance block（本 run 范围）| 1 |
| 已覆盖 | 1 |
| `make check` | PASS（exit 0）|

一句话结论：Phase C 以文档约定 + `doc check` 的 `telemetry_hint` 字段落地 Agent 自评入口；WAL record 仍 fail-open，不 gate stage。

## Intent 覆盖表

| Intent / Safety | 层级 | Task ID | 实现位置 | Test | 状态 |
|---|---|---|---|---|---|
| `TelemetryRecordFailOpen` | acceptance | T-TEL-0002 | `crates/telemetry/src/lib.rs::run_tool` + `AGENT_TELEMETRY.md` | `telemetry::integration::record_and_flush_fail_open_without_endpoint` | ✅ |
| Agent score hint | 约定（非 intent） | T-TEL-0002 | `telemetry_bridge::score_hint` + `workspace.rs::check_doc` | `telemetry_bridge::tests::score_hint_includes_run_and_doc` | ✅ |
| Workflow Rule 12 | AGENTS.md | — | `AGENTS.md` §Workflow Rules #12 | 人工审阅 | ✅ |
| Skill guide 观测小节 | guide | — | `shadow/equiv/living/issue-author` guide 末尾 | 人工审阅 | ✅ |
| SPAN 字段表 | 文档 | — | `products/telemetry/SPAN_SCHEMA.md` | — | ✅ |

状态：✅ 已覆盖 / ⚠️ 部分 / ❌ 缺失

## File Manifest 对账

| 路径 | 预期责任 | 磁盘存在 | 备注 |
|---|---|---|---|
| `products/telemetry/SPAN_SCHEMA.md` | span 字段与命令模板 | ✅ | 新建 |
| `products/telemetry/AGENT_TELEMETRY.md` | Agent 约定摘要 | ✅ | 新建 |
| `products/telemetry/ARCHITECTURE.md` | Phase C 完成标记 | ✅ | 更新 |
| `products/telemetry/PRODUCT.md` | 链到 AGENT/SPAN | ✅ | 更新 |
| `products/telemetry/tasks/daily-ops/T-TEL-0002-*.md` | task 成功标志 | ✅ | 含 telemetry_hint |
| `AGENTS.md` | Workflow Rule 12 | ✅ | |
| `intent-coder/skills/*/guide.md` | Agent 观测小节 | ✅ | 4 个 skill |
| `crates/cli-ux/src/telemetry_bridge.rs` | `score_hint` | ✅ | |
| `crates/cli-ux/src/workspace.rs` | `check_doc` → `telemetry_hint` | ✅ | passed 时注入 |

## cargo test

```
make check — PASS
  telemetry_bridge::tests::score_hint_includes_run_and_doc ... ok
  telemetry::integration::record_and_flush_fail_open_without_endpoint ... ok
test result: ok. 全部 workspace tests passed
```

## 待办

| 项 | 类型 | 跟进 |
|---|---|---|
| Phase D run 聚合报告 | 产品 | 新 Issue（T-TEL-0003+） |
| weekly 反馈闭环 | 活文档 | doc-sync-weekly |

## 检查清单

- [x] 每个 acceptance block 在表中有行
- [x] 每个 ✅ 行有具体路径与 test 名
- [x] File Manifest 与 Phase C 范围一致
- [x] make check 已实跑并记录 exit code
- [x] 待办项未冒充 ✅
