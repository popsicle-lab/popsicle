---
id: doc-193
doc_type: shadow-implementer
title: Agent Runtime P0 实现覆盖
pipeline_run_id: 00000050-0000-4050-8005-50000000000050
artifact: implementation-coverage
slug: agent-runtime-p0
generated_by: shadow-implementer
slice: agent-runtime
last_updated: 2026-07-06
crate: crates/agent-daemon/ + crates/agent-server/ + crates/cli-ux/
cargo_test_exit: 0
intent_blocks_total: 4
intent_blocks_covered: 2
query_anchors:
  - "agent-runtime P0 哪些 intent 已有代码？"
  - "daemon 与 server 测试在哪？"
---
# 实现覆盖报告 — agent-runtime P0

> PROJ-82 · T-AR-0001 / T-AR-0002 / T-AR-0006 · ADR-001 File Manifest

## Scope Checklist

- [x] target slice 已确认（agent-runtime）
- [x] 全部 intent block 已枚举（acceptance 4 block）
- [x] File Manifest 路径清单已列出
- [x] 每个 block 已标 已实现/部分/缺失
- [x] 跨界端口归属已核对（cli-ux thin shell；FSM 仍在 skill-runtime）

## Summary

| 指标 | 值 |
|---|---|
| Slice | agent-runtime |
| Crate | `agent-daemon` + `agent-server` + `cli-ux/src/daemon.rs` |
| acceptance block 总数 | 4 |
| 已覆盖（fn 或 test）| 2（P0 范围）；2 待 P1 |
| `cargo test -p agent-daemon -p agent-server` | PASS（exit 0）|
| `make check` | PASS |

一句话结论：P0 交付 Daemon 注册（T-AR-0001）、自托管 Server 派活/认领（T-AR-0006）、首次派活 E2E 路径（T-AR-0002，`issue start` subprocess）；离线拒绝与手机审批留 P1。

## Intent 覆盖表

| Intent / Safety | 层级 | Task ID | 实现位置 | Test | 状态 |
|---|---|---|---|---|---|
| `RuntimeRegistersWhenDaemonStarts` | acceptance | T-AR-0001 | `agent-daemon::RuntimeStatus::probe`；`cli-ux/daemon.rs::daemon_start` | `agent-daemon::runtime_status_roundtrip`；`invoker_runs_doctor_when_binary_exists` | ✅ |
| `DispatchQueuedWhenRuntimeOnline` | acceptance | T-AR-0002 | `agent-server::TaskStore::dispatch`；`POST /v1/dispatch`；`agent-daemon::ServerClient::claim` + `execute_issue_start` | `agent-server::dispatch_then_claim` | ✅ |
| `DispatchRejectedWhenRuntimeOffline` | acceptance | T-AR-0005 | —（Server 尚无 runtime 注册表） | — | ❌ P1 |
| `ApprovalCreatesConfirmTask` | acceptance | T-AR-0004 | —（无 confirm-task API） | — | ❌ P1 |

状态：✅ 已覆盖 / ⚠️ 部分 / ❌ 缺失

## File Manifest 对账

| ADR-001 路径 | 预期责任 | 磁盘存在 | 备注 |
|---|---|---|---|
| `crates/agent-server/` | REST 派活/认领/查询 | ✅ | 内存 TaskStore；默认 :8787 |
| `crates/agent-daemon/` | popsicle subprocess + poll | ✅ | `PopsicleInvoker`、`ServerClient` |
| `crates/cli-ux/src/daemon.rs` | `popsicle daemon *` | ✅ | start/stop/status/logs |
| `deploy/agent-runtime/docker-compose.yml` | 自托管 Server | ✅ | P0 仅 agent-server 单服务 |
| `deploy/agent-runtime/Dockerfile` | 镜像构建 | ✅ | `cargo build -p agent-server` |

## cargo test

```
cargo test -p agent-daemon -p agent-server
test result: ok. 3 passed; 0 failed

make check
(fmt + clippy -Dwarnings + test) — exit 0
```

## E2E 验证（curl）

```bash
# 终端 1
cargo run -p agent-server

# 终端 2
curl -s -X POST http://127.0.0.1:8787/v1/dispatch \
  -H 'Content-Type: application/json' \
  -d '{"workspace_id":"popsicle","runtime_id":"dev1","issue_key":"PROJ-82","pipeline":"feature-delivery"}'

# 终端 3（认领并 issue start，需 AGENT_RUNTIME_SERVER_URL）
export AGENT_RUNTIME_SERVER_URL=http://127.0.0.1:8787
export AGENT_RUNTIME_ID=dev1
./target/debug/popsicle daemon start --foreground
```

## 待办

| 项 | 类型 | 跟进 |
|---|---|---|
| Runtime 注册表 + 离线拒绝 | intent Block 3 | P1 Server PG + heartbeat |
| 手机审批 confirm-task | intent Block 4 | P1 Mobile + WS |
| `pipeline next` 自动推进 | Daemon 执行循环 | P1 shadow 续跑 |
| PostgreSQL 持久化 | T-AR-0006 完整自托管 | P1 |

## 检查清单

- [x] 每个 acceptance block 在表中有行
- [x] 每个 ✅ 行有具体 `文件::符号` 与 test 名
- [x] File Manifest 与 ADR-001 Consequences 一致
- [x] cargo test 已实跑并记录 exit code
- [x] 待办项未冒充 ✅
