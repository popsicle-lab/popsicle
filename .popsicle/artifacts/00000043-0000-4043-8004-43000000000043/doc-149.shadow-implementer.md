---
id: doc-149
doc_type: shadow-implementer
title: Telemetry MVP 实现覆盖
artifact: implementation-coverage
slug: telemetry-mvp-delivery
pipeline_run_id: 00000043-0000-4043-8004-43000000000043
slice: telemetry
crate: crates/telemetry/
cargo_test_exit: 0
intent_blocks_total: 3
intent_blocks_covered: 3
query_anchors:
  - "telemetry intent 哪些已经有代码了？"
  - "tool run telemetry 在哪里接线？"
---

# 实现覆盖报告 — telemetry-mvp-delivery

> PROJ-68 · T-TEL-0001/0002/0003 MVP 交付（ADR-001 File Manifest）。

## Scope Checklist

- [x] target slice 已确认（telemetry + cli-ux inject）
- [x] 全部 intent block 已枚举
- [x] File Manifest 路径清单已列出
- [x] 每个 block 已标 已实现/部分/缺失
- [x] 跨界端口归属已核对（Sink 在 telemetry，inject 在 cli-ux）

## Summary

| 指标 | 值 |
|---|---|
| Slice | telemetry |
| Crate | `crates/telemetry/` + `crates/cli-ux/src/telemetry_bridge.rs` |
| acceptance + invariants block 总数 | 3 |
| 已覆盖（fn 或 test）| 3 |
| `cargo test -p telemetry` | PASS（exit 0）|
| `make check` | PASS |

一句话结论：MVP 旁路 telemetry 已落地 — WAL append、fail-open record/flush、cli-ux 编排 span 与 `tool run telemetry`；OTLP 为 HTTP JSON MVP（无 protobuf 后台线程，见待办）。

## Intent 覆盖表

| Intent / Safety | 层级 | Task ID | 实现位置 | Test | 状态 |
|---|---|---|---|---|---|
| `OtlpConfigRoundTrips` | acceptance | T-TEL-0001 | `config.rs::load_config` | `config::tests::default_config_is_fail_open` | ✅ |
| `TelemetryRecordFailOpen` | acceptance | T-TEL-0002 | `lib.rs::record_span`, `run_tool` exit 0 | `integration::record_and_flush_fail_open_without_endpoint` | ✅ |
| `OrchestrationSpanOnIssueStart` | acceptance | T-TEL-0003 | `telemetry_bridge.rs::orchestration_span` + `workspace.rs` issue.start/doc.create/stage.complete | 手工：`issue start` / `doc create` 写 WAL | ✅ |
| `TelemetrySinkFailOpen` | contracts | — | `lib.rs::record_span` Degraded 路径 | integration test | ✅ |
| `TelemetryNeverBlocksMain` | invariants | — | cli-ux 忽略 record 返回值 | `make check` | ✅ |

## File Manifest 对账

| ADR-001 路径 | 预期责任 | 磁盘存在 | 备注 |
|---|---|---|---|
| `crates/telemetry/` | WAL + config + export | ✅ | 新建 |
| `crates/cli-ux/Cargo.toml` | dep telemetry | ✅ | |
| `crates/cli-ux/src/telemetry_bridge.rs` | inject + tool | ✅ | |
| `crates/cli-ux/src/workspace.rs` | orchestration hooks | ✅ | issue start / doc create / stage complete |
| `.gitignore` | `.popsicle/telemetry/` | ✅ | |
| `AGENTS.md` | tool run telemetry | ✅ | |

## cargo test

```
cargo test -p telemetry
test result: ok. 3 passed; 0 failed

make check
（fmt + clippy + 全 workspace test）PASS
```

## 待办

| 项 | 类型 | 跟进 |
|---|---|---|
| OTLP protobuf + 后台 flush 线程 | P1 | 新 Issue |
| skill-runtime pipeline session span | P1 | T-TEL-0003 扩展 |
| `popsicle admin` otel 配置子命令 | P2 | T-TEL-0001 完善 |

## 检查清单

- [x] 每个 acceptance block 在表中有行
- [x] 每个 ✅ 行有具体 `文件::符号` 与 test 名
- [x] File Manifest 与 ADR Consequences 一致
- [x] cargo test 已实跑并记录 exit code
- [x] 待办项未冒充 ✅
