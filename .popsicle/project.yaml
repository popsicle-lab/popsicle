version: 1
agent:
  language: zh-cn
paths:
  products_dir: products
  default_product: ''
workflow:
  profile: daily-dev
  sync_agents_md: true
  inject_on_run: true
  approval_mode: delegate-dangerous
git:
  track_workspace: true
